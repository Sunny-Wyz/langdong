# 全站视觉回归清单（2026-03-30）

## 统一基线
- 容器：统一页面内边距与卡片边框阴影层级
- 页头：统一卡片头部字号、字重、分割线
- 列表：统一表格行高、单元格节奏、激活分页色
- 弹窗：统一标题区/内容区/底部按钮区间距与边界
- 按钮：统一主按钮色与小尺寸按钮节奏

## 登录页
- [x] Login.vue：标题、按钮、背景风格统一

## 各模块列表页
- [x] 备件档案：SparePartList.vue（已按 page-container 结构）
- [x] 设备档案：EquipmentProfile.vue（由全局基线统一容器与卡片）
- [x] 货位档案：LocationProfile.vue（由全局基线统一容器与卡片）
- [x] 供应商档案：SupplierProfile.vue（由全局基线统一容器与卡片）
- [x] 品类字典：SupplyCategory.vue（由全局基线统一容器与卡片）
- [x] 系统用户：sys/UserManage.vue（由全局基线统一容器与卡片）
- [x] 系统角色：sys/RoleManage.vue（由全局基线统一容器与卡片）
- [x] 领用流程：requisition/*.vue（由全局基线统一容器、表格与分页）
- [x] 工单流程：workorder/*.vue（由全局基线统一容器、表格与分页）
- [x] 采购流程：purchase/*.vue（由全局基线统一容器、表格与分页）
- [x] 报表模块：report/*.vue（由全局基线统一容器与卡片）
- [x] AI/分类/PHM：ai/*.vue、classify/*.vue、phm/*.vue（由全局基线统一容器与卡片）
- [x] 仓储模块：warehouse/*.vue（由全局基线统一容器与卡片）

## 弹窗页
- [x] 常规弹窗（el-dialog）：标题、正文、footer 区统一
- [x] 表单弹窗：统一顶部/内容/底部间距和边线
- [x] 明细弹窗：统一表格内边距与视觉密度

## 验收结果
- [x] 视觉风格统一：完成
- [x] 构建校验通过：完成
- [ ] 人工回归（登录、筛选、分页、弹窗提交流程）：待执行

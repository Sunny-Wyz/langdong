<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langdong.spare.mapper.SparePartStockMapper">

    <select id="findBySparePartId" resultType="com.langdong.spare.entity.SparePartStock">
        SELECT id, spare_part_id as sparePartId, quantity, updated_at as updatedAt
        FROM spare_part_stock
        WHERE spare_part_id = #{sparePartId}
    </select>

    <select id="findAll" resultType="com.langdong.spare.entity.SparePartStock">
        SELECT s.id, s.spare_part_id as sparePartId, s.quantity, s.updated_at as updatedAt,
               sp.code as sparePartCode, sp.name as sparePartName
        FROM spare_part_stock s
        LEFT JOIN spare_part sp ON s.spare_part_id = sp.id
        ORDER BY s.updated_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO spare_part_stock (spare_part_id, quantity)
        VALUES (#{sparePartId}, #{quantity})
    </insert>

    <update id="addQuantity">
        UPDATE spare_part_stock 
        SET quantity = quantity + #{addedQuantity} 
        WHERE spare_part_id = #{sparePartId}
    </update>

</mapper>

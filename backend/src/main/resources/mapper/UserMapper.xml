<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langdong.spare.mapper.UserMapper">
    <select id="findByUsername" resultType="User">
        SELECT id, username, name, password, status, created_at as createdAt, updated_at as updatedAt 
        FROM user 
        WHERE username = #{username}
    </select>

    <select id="findAll" resultType="User">
        SELECT id, username, name, status, created_at as createdAt, updated_at as updatedAt 
        FROM user 
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, name, password, status)
        VALUES (#{username}, #{name}, #{password}, #{status})
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>

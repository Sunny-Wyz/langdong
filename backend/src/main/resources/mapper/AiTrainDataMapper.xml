<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langdong.spare.mapper.AiTrainDataMapper">

    <resultMap id="AiTrainDataRecordMap" type="com.langdong.spare.entity.AiTrainDataRecord">
        <id property="id" column="id"/>
        <result property="bizDate" column="biz_date"/>
        <result property="sparePartId" column="spare_part_id"/>
        <result property="partCode" column="part_code"/>
        <result property="dailyOutboundQty" column="daily_outbound_qty"/>
        <result property="dailyRequisitionApplyQty" column="daily_requisition_apply_qty"/>
        <result property="dailyRequisitionOutQty" column="daily_requisition_out_qty"/>
        <result property="dailyInstallQty" column="daily_install_qty"/>
        <result property="dailyWorkOrderCnt" column="daily_work_order_cnt"/>
        <result property="dailyPurchaseArrivalQty" column="daily_purchase_arrival_qty"/>
        <result property="dailyPurchaseArrivalOrders" column="daily_purchase_arrival_orders"/>
        <result property="dayOfWeek" column="day_of_week"/>
        <result property="isWeekend" column="is_weekend"/>
        <result property="sourceLevel" column="source_level"/>
        <result property="isImputed" column="is_imputed"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="WhereCondition">
        <where>
            <if test="startDate != null and startDate != ''">
                AND biz_date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND biz_date &lt;= #{endDate}
            </if>
            <if test="partCode != null and partCode != ''">
                AND part_code LIKE CONCAT('%', #{partCode}, '%')
            </if>
            <if test="sourceLevel != null and sourceLevel != ''">
                AND source_level = #{sourceLevel}
            </if>
            <if test="isImputed != null">
                AND is_imputed = #{isImputed}
            </if>
        </where>
    </sql>

    <select id="findByPage" resultMap="AiTrainDataRecordMap">
        SELECT
            id, biz_date, spare_part_id, part_code,
            daily_outbound_qty, daily_requisition_apply_qty, daily_requisition_out_qty,
            daily_install_qty, daily_work_order_cnt, daily_purchase_arrival_qty,
            daily_purchase_arrival_orders, day_of_week, is_weekend,
            source_level, is_imputed, created_at, updated_at
        FROM ai_part_daily_train_data
        <include refid="WhereCondition"/>
        <choose>
            <when test="orderBy == 'biz_date_desc'">
                ORDER BY biz_date DESC, part_code ASC
            </when>
            <otherwise>
                <!-- 默认：有量优先，避免首页全是 0 -->
                ORDER BY daily_outbound_qty DESC, biz_date DESC, part_code ASC
            </otherwise>
        </choose>
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByPage" resultType="long">
        SELECT COUNT(*)
        FROM ai_part_daily_train_data
        <include refid="WhereCondition"/>
    </select>

    <select id="selectMeta" resultType="java.util.HashMap">
        SELECT
            COUNT(*) AS totalRows,
            COUNT(DISTINCT part_code) AS distinctParts,
            MIN(biz_date) AS minBizDate,
            MAX(biz_date) AS maxBizDate,
            MAX(updated_at) AS lastUpdatedAt,
            SUM(CASE WHEN daily_outbound_qty &gt; 0 THEN 1 ELSE 0 END) AS nonzeroOutboundRows,
            SUM(CASE WHEN is_imputed = 1 THEN 1 ELSE 0 END) AS imputedRows,
            SUM(CASE WHEN source_level = 'NONE' THEN 1 ELSE 0 END) AS noneSourceRows,
            MAX(CASE WHEN daily_outbound_qty &gt; 0 THEN biz_date ELSE NULL END) AS lastDemandDate
        FROM ai_part_daily_train_data
    </select>

    <delete id="deleteOutsideWindow">
        DELETE FROM ai_part_daily_train_data
        WHERE biz_date &lt; #{startDate}
           OR biz_date &gt; #{endDate}
    </delete>

</mapper>
